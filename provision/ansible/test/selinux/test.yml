##############################################################################
# Name        : test.yml
# Creator     : L.S.H.
# Syntax      : ansible-playbook test.yml
# Description :
##############################################################################
---
- name: ansible feature test playbook 
  hosts: localhost
  become_method: sudo
  become: yes
  remote_user: pcp
  connection: local
  tasks:
    ####################################
    # selinux desabled
    ####################################
    - name: Disable SELinux
      selinux:
        state: disabled
    
    ####################################
    # selinux desabled
    ####################################
    - name: Disable SELinux
      service:
        name: firewalld
        state: stopped
        enabled: False
          
