##############################################################################
# Name        : test.yml
# Creator     : L.S.H.
# Syntax      : ansible-playbook test.yml
# Description :
##############################################################################
---
- name: Create Azure VM
  hosts: localhost
  become_method: sudo
  become: yes
  remote_user: pcp
  connection: local
  tasks:
    - name: "Change Hostname on server" 
      hostname:
        name: "SpongeBob-Ansible"

