---
- hosts: localhost
  tasks: 
    - name: 1. inventory file loop test 
      debug: 
        msg: "{{ item }}"
      loop: "{{ query('inventory_hostnames', 'SpongeBobVM1') }}"

    - name: 2. inventory_hostname  
      debug: 
        msg: "{{ inventory_hostname }}" 
      when: "AnsibleSvr in inventory_hostname"


